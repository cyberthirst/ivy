[project]
name = "ivy"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "vyper @ git+https://github.com/vyperlang/vyper.git@v0.4.1b2",
    "ruff>=0.6.4",
    "pre-commit>=3.8.0",
    "pytest>=8.3.2",
    "eth-utils>=5.0.0",
    "hypothesis>=6.114.1",
    "rlp>=4.0.1",
    "pytest-cov>=5.0.0",
    "pytest-xdist>=3.6.1",
    "pytest-instafail>=0.5.0",
]


[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.metadata]
allow-direct-references = true