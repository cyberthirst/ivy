[project]
name = "ivy"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "vyper @ git+https://github.com/cyberthirst/vyper.git@vyzzer-patch",
    "ruff>=0.6.4",
    "pre-commit>=3.8.0",
    "pytest==7.4.3",
    "eth-utils==4.1.1",
    "hypothesis==6.104.2",
    "rlp==4.0.1",
    "pytest-cov==4.1.0",
    "pytest-xdist>=3.6.1",
    "pytest-instafail>=0.5.0",
]


[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.metadata]
allow-direct-references = true
